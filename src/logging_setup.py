"""logging_setup — logger JSON multi-niveaux + redact() PII.

Placeholder (à implémenter en Phase 1). Exigence RGPD : aucune PII
(email / tel / nom) dans les logs ni les messages d'erreur. La fonction
redact() masque les champs sensibles avant toute écriture de log.
"""
